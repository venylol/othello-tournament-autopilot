{
  "name": "onlicheck-static",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name onlicheck"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
